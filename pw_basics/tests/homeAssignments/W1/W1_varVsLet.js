const browserVersion = 'Chrome'

function getBrowserVersion(browser)
{
    if(browserVersion === 'Chrome')
    {
        let browserVersion =  browser
    }
    console.log(browserVersion)
}

console.log(getBrowserVersion('Chrome'))